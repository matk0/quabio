# Secrets for Mito application at knowledgebase.studio
# Secrets defined here are available for reference under registry/password, env/secret, builder/secrets,
# and accessories/*/env/secret in config/deploy.yml. All secrets should be pulled from either
# password manager, ENV, or a file. DO NOT ENTER RAW CREDENTIALS HERE! This file needs to be safe for git.

# Using 1password (adapt to your setup):
SECRETS=$(kamal secrets fetch --adapter 1password --account WI6E6XUXN5ER3BCAAI3MF6AXII --from knowledgebase.studio/Production KAMAL_REGISTRY_PASSWORD RAILS_MASTER_KEY DB_HOST DATABASE_URL OPENAI_API_KEY)
KAMAL_REGISTRY_PASSWORD=$(kamal secrets extract KAMAL_REGISTRY_PASSWORD ${SECRETS})
RAILS_MASTER_KEY=$(kamal secrets extract RAILS_MASTER_KEY ${SECRETS})
DB_HOST=$(kamal secrets extract DB_HOST ${SECRETS})
DATABASE_URL=$(kamal secrets extract DATABASE_URL ${SECRETS})
OPENAI_API_KEY=$(kamal secrets extract OPENAI_API_KEY ${SECRETS})

# Alternative: Manual secrets (uncomment and fill in):
# KAMAL_REGISTRY_PASSWORD=your-dockerhub-password
# RAILS_MASTER_KEY=$(cat config/master.key)
# DB_HOST=your-postgresql-host
# DATABASE_URL=postgresql://user:pass@host/mito_production
# OPENAI_API_KEY=sk-your-openai-key
